name: astrbot_plugin_artist # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 画图 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 一次性 /draw 画图；按用户 OpenID 设置每日次数，不依赖会话 LLM。
version: 2.1.1
author: Soulter # 作者
repo: https://github.com/204343414/astrbot_plugin_artist
